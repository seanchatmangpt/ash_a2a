defmodule AshA2A.Test.PubSubFixture do
  @moduledoc """
  Named registration for the real `Phoenix.PubSub` process started by
  `AshA2A.RuntimeProvidersIntegrationTest`'s Topology setup and consumed by
  `AshA2A.Test.PresenceFixture`.
  """
end
