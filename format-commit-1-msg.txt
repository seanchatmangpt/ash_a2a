test(ci): name-and-print exclusions for tests whose real collaborator is a local-only checkout

ash_a2a#27 run 35539819401: 107 mix test failures on a hosted runner. 103 are
the real praxis-graphlaw wasm being unreachable (seven independent resolvers
default to a path under the author's home: WasmDriver, GraphLaw.Wasm,
GraphLaw.Runtime, Semantic.GraphLawBridge, Semantic.GraphLaw.Wasm,
SA2A.Graphlaw, RootManifest.EngineProbe); 3 are the 11-repo topology courts
looking for sibling checkouts under /Users/sac; 1 is a real defect fixed
elsewhere (SemanticRefusalTest, see errc/ash-a2a-pr27-refusal-classes).

test/test_helper.exs now accumulates excluded tags. :graphlaw is unchanged.
New :graphlaw_engine is excluded when any resolved wasm path or node is
missing; new :sibling_repos is excluded when any of the 11 topology repos has
no .git. Every exclusion prints its reason on stderr. Nothing is stubbed and
nothing passes silently, the same contract :graphlaw already had.

22 test files gain @tag/@moduletag on exactly the tests that failed on the
runner (AdversarialInputTest and SemanticCrossPeerTest module-wide).

Verification (hosted-CI simulation: wasm paths pointed at a missing file via
app config plus all five env vars, over the 23 failing files):
  forced include:  316 tests, 103 failures (CI set minus the 4 that cannot
                   reproduce locally, 0 failures CI did not have)
  as committed:    4 doctests, 213 tests, 0 failures, 3 skipped (122 excluded)
Sibling-repo simulation (:chicago_topology_root -> missing dir):
  forced include:  19 tests, 3 failures
  as committed:    16 tests, 0 failures (3 excluded)
The temporary simulation config was reverted; config/test.exs is unchanged.
